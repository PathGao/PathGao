# 我的工程哲学

> 一份写给自己、也写给未来接手的人的判断清单。
> 每一条都来自一次真实的踩坑——写代码、搭系统、查 bug、给人讲东西的时候。

*活文档,持续修订。*

---

## 引子 · 一个装不下的矛盾

面对任何一个稍微复杂点的问题,我想同时看住的是这些:

```
        现在到底是什么 ──── 哪些变量在动 ──── 谁因谁果
               │                 │                │
          断点在哪 ─────── 谁该负责 ─────── 长期会怎样

                          ▼  全部塞进脑子  ▼

                      ┌──────────────────┐
                      │  实际握得住的量   │ ← 有上限
                      └──────────────────┘   而且一累就缩水
```

想看住的永远比握得住的多。**这是矛盾的根源,而且它不会消失。**

出路只有两条:

```
  A  训练自己握更多      ── 有天花板,而且状态一差就失效
  B  让每样东西本身更轻  ── 没有天花板         ★ 这份文档全在讲 B
```

选 B。剩下的全是它在四个场合的做法——**学、建、修、讲**。

```
                  ┌─────────────────────────────┐
                  │   唯一的尺子:认知负荷        │
                  │  "读懂它,要同时握住多少?"    │
                  └──────────────┬──────────────┘
                                 │  同一把尺,量四件事
      ┌────────────┬─────────────┼─────────────┬────────────┐
      ▼            ▼             ▼             ▼
    【学】       【建】         【修】        【讲】
  拿到新东西   从零搭系统    出事的时候    讲给别人听
      │            │             │             │
  先全后减     先少后加     修故障,不修触发  一次只上一层
  见过才会选   痛过才配有   查出来 > 想出来  讲之前先复述
```

---

## 〇 · 尺子:认知负荷

> 认知负荷 = 你为了做成一件事,脑子里必须同时握着多少东西。

这不是一个能算出数字的量。**它是一把相对的尺子**——用来比"这样写"和"那样写"哪个更沉,不是用来给谁打分。真正有用的只有一句:**装不下的时候你自己知道,那种发懵就是超载。**

分两种:

| | 从哪来 | 能不能砍 |
|---|---|---|
| **内在负荷** | 事情本身就难 | 砍不掉,这是活儿本身 |
| **外在负荷** | 难在它被摆成了这个样子 | **几乎全能砍** |

下面所有讨论只针对第二种。它长这样:

```
 问题:"这是哪一个程序?"

   系统里三个部件,各按不同的东西回答:
       甲 —— 按【安装路径】
       乙 —— 按【程序 ID】
       丙 —— 按【代码签名】

   同一个问题,三个可能互相矛盾的答案。
   每次想它都要把三套规则同时握着              🤯

   统一成一套之后,同一个问题只有一个答案       🧠
```

三条规则单看都不难。难在它们被摆成了会互相打架的样子。**这是外在负荷,而它是我自己造的。**

*(`🧠` = 脑子空着,`🤯` = 装不下了。标的是相对的沉重,不是计数)*

### 深的东西少,浅的东西多

> 好零件:**功能强,接口简单**。坏零件:**功能没多少,接口一堆**。

Unix 读写文件只有五个调用——`open` `read` `write` `lseek` `close`,背后是几十万行代码。功能巨大,接口五个词,所以人人都会用。

浅零件一多,你不但要记住每个零件干什么,**还要记住它们之间怎么连**。零件数是加法,连接数是乘法。

所以我给一切交付物的标准是三个词:**接口浅、层次少、残留清**。

### 熟悉不等于简单

这两个感觉一模一样——都让你不费劲,但原因完全不同。你写的每一个"聪明"技巧,别人都要额外学一次;学会之后他也不觉得难了。**这就是为什么你最看不出自己的东西哪里该简化。** 找个新人来读,他连续四十分钟还在懵,不是他的问题。

---

## 一 · 学:先见全貌,再做减法

> **你没法给一个还不理解的东西做减法。**

```
 最省力地先把完整版用起来  →  感受到它能干什么  →  再减到自己真用的那几样
```

三段顺序不能换。跳过第一段直接定制,你做的是**基于想象的减法**。

### 反直觉在哪

```
 默认路径(多数教程、多数人的直觉)
    从最小开始 ──► 缺什么加什么
         └ 隐含假设:你说得出自己缺什么
           实际:你没见过"全",所以想象不出缺口
         └ 结果:一路撞墙,撞一次学一次
           而且每次撞墙时你都在赶工,没心情探索

 我的路径
    从"全都有"开始 ──► 用过 ──► 知道真正用哪几样 ──► 减到那几样
         └ 减法基于经验,不基于想象
```

关键差别不在起点高低,**在于你凭什么做取舍**。

### 两条边界

**边界一 · 目标是"搞懂原理"时,反过来。** 完整版的封装恰恰会挡住机制——界面上点一下就换好了,你永远不会知道它改的是哪个文件的哪一行。两个目标不冲突,只是顺序不同:先见全貌知道自己要什么,再回底层把那几样搞懂。

**边界二 · 完整版会锁住你时,打折。** 用之前先问一句:**如果我三个月后要换掉它,我带得走什么?** 数据是纯文本、换界面不换数据 → 判据全额成立;私有格式、账号绑定、导出困难 → "先用后减"会变成"迁移",而迁移的痛苦可能超过一开始多学一点。

### 和稳态判据的关系

```
 认知负荷判据   衡量【稳态】:长期用这套东西,脑子里要同时握几样
 先见全貌判据   衡量【路径】:从零走到稳态,哪条路总共最省脑子
```

两者在起点上冲突——完整版一上来更累。但那是**一次性预付**,换来后面每一次取舍都有依据。"从最小开始"把这份累摊到每一次撞墙上,总量更高,而且发生在最糟的时刻。

---

## 二 · 建:从零搭一个能维护的地基

上一章是**学**的时候用的。这一章是**建**的时候用的,方向正好相反:

```
 学:先全 → 后减      你需要【见过】才知道有哪些选项
 建:先少 → 后加      你需要【痛过】才配拥有一个零件
```

为什么反?因为成本结构不同。学的时候,完整版的代价是一次性的(装上、看一眼、卸掉);建的时候,**每个零件的代价是长期的**——维护它、升级它、它坏了要查它。

> 一句话:**看的时候要全,留的时候要少。**

### 2.1 加任何东西之前,先爬这个梯子

停在第一个够用的台阶上:

```
 ① 这东西需要存在吗?          还没真实需求 → 不做,一句话说清为什么
 ② 手上已经有了吗?            项目里已有的工具/组件/写法 → 复用
 ③ 标准库能做吗?              能就用
 ④ 平台原生的能做吗?          浏览器自带的日期控件 > 一个日期选择器库
                              数据库的约束 > 应用层再判一次
 ⑤ 已经装着的依赖能做吗?      能就用,不为几行代码新加一个
 ⑥ 能一行写完吗?              那就一行
 ⑦ 到这里才开始写新东西        写能跑的最小版本
```

**最常见的浪费不在第七级,在第二级**——重新实现一个隔壁文件里已经有的东西。写之前先看一眼。

> 适度的复制,好过引入一个新依赖。—— Rob Pike

> **你的依赖库,也是你的代码。** 出了 bug,你要顺着它十几层调用链往下翻。

### 2.2 地基:先画出"哪些零件跟地基绑死"这条线

**① 底座用官方的,别用整合包。** 整合包把一切都替你预置好了,代价是你不知道它改了什么、也回不去。官方底座 + 自己一个个装 = **每样东西都是你放进去的,坏了知道找谁。**

**② 分清绑死的和独立的。**

```
 跟地基绑死  ── 版本必须严格对上   → 只能整体换,绝不单独升
 独立零件    ── 谁也不认           → 随便升,坏了单独换
```

例:一台机器上,驱动那类东西和系统内核绑死,普通应用不是。所以"刷新可安装列表"随时可以,**"全部升级"绝对不行**——它会把绑死的那类升到和当前内核对不上,一次重启就再也起不来。

**任何系统里都有一小撮必须跟地基同时换的东西。搭之前先把这条线画出来——画不出来,你的每次升级都是赌博。**

**③ 信任分级的判据,是"装错了还能不能救回来"。**

不是"这个来源可不可靠"。第三方商店可以用来装普通应用,但不能用来装驱动、防火墙、域名解析——不是因为它不可信,是因为那几样一旦装错,你面对的是一台连登都登不进去的砖。

### 2.3 搭的过程

```
 装一组 ─► 验一组 ─► 再装下一组
```

不为仪式感,是为了**出问题时嫌疑范围只有刚装的那一组**。一口气装完二十个再发现起不来,你要从二十个里找。

**启用顺序是依赖顺序,不是重要性顺序。** 存储能挂上 → 能写进去 → 上层服务能读写 → **最后**才打开那个会接管全局的开关。每一步都踩在上一步**已经验证过**的地上。

**最危险的最后开,而且开之前先想好怎么关。** 一个接管全局流量的开关配错了,就是整个网络断掉、连进去改配置的页面都打不开。退路必须在动手之前就写好。**没有退路的功能不要开。**

**别用"强制安装"绕过依赖检查。** 装不上是在告诉你"这个组合没人验证过"。强行装上,你只是把报错从安装时挪到了运行时——那时你已经忘了自己动过什么。遇到不兼容,正确动作是**等**。

### 2.4 结构:三条会一直复利的切法

**① 一件事只能有一个权威。**

一个编辑器,关掉时记住你开着哪几个文件,下次自动恢复。为了恢复得快,那份记录里顺手也存了文件内容——于是同一份内容有了两个出处:

```
 磁盘上的文件        ← 权威一
 记录里存的那一份    ← 权威二

 关掉 ── 中间这段时间,磁盘上的文件可能被别人改了 ── 重开
      → 恢复出记录里的【旧内容】
      → 你敲一个字,自动保存被触发
      → 旧内容盖掉了新文件  💀
```

修法不是加锁、不是加校验,是**让记录不带内容**——磁盘是唯一权威,记录只管"开着哪几个"。

反过来也成立。同一个程序里,把一个标签拖到新窗口的搬运包**可以**带内容:原件一直活到新窗口确认收到为止,中间没有"关机时间",第二份活不过一秒。

> **同一个判据,答案相反,差别只在时间。两个权威之间隔多久,就有多危险。**

**② 切分切对了,后面的功能全是免费的。**

还是那个编辑器。我只做了一个决定:

```
 "还没保存的内容"  → 归关窗时那个提醒对话框管
 "开着哪几个标签"  → 归那份记录管
```

然后没设计过的好处自己滚了出来:记录从几 MB 掉到几百字节;新建但一个字没写的空标签天然不进记录;有文件名的标签和没起名的编号标签活在两套命名里,**永远撞不上**。

> **好的切分让后来的功能自动继承边界;坏的切分让每个功能都要单独打补丁。**

**③ 守那扇绕不开的门,并且让失败喊出声。**

想上规矩,别在每个调用点放一道岗——找到**所有路径都绕不开的那一扇门**。能触发保存的地方有七八处,但它们最后都汇进同一个写盘函数;那一个函数里的一行日志,就分清了"存的时候就错了"和"存对了后来被覆盖"——两种情况的排查方向是相反的。

另一面:**提供一条正确的路 ≠ 堵死错误的路。** 做了漂亮的封装,别指望"大家自然会用"。要问的是——**约定失守的时候,谁会喊一声?** 有人喊,它是纪律;没人喊,它只是建议。

而不喊的失败最贵:

```
 权限没配 → 抛异常 → 被上层吞掉 → 界面看起来"点了没反应"
 消息寄给了一个刚退出的进程 → 文件就是没打开,一句提示都没有
```

崩溃是好事,它告诉你出事了、在哪出的。**设计的时候就要问:这条路走不通时,谁会知道?**

---

## 三 · 修:出事的时候

### 3.1 修故障模式,不要修触发条件

一个界面元素有时重叠、有时空一块的 bug。观察下来它受四个因素影响:元素多不多、尺寸变化大不大、内容分几批加载、是不是在屏幕外。

很容易掉进去调这四个参数,让它不那么容易发作。但——

```
 那四个因素 ── 不制造缺陷,只决定它多久现形、现形多大
 真正的故障 ── "内容变了→标记要重算→上报→重新计算"这条链断了一环
              一个过时的尺寸留在了缓存里
```

修法是在那条链**外面**放一个对账员:注册一个"布局一变就会被叫醒"的观察器,醒来量一次真实尺寸,写回**绝对值**。它不在链上,链上任何抄近路的优化都绕不过它;写绝对值不写增量,所以重复多少次结果都一样。

```
 改之前:出不出事 = 触发条件 × 【失败概率】   ← 只能碰运气
 改之后:出不出事 = 触发条件 × 【通知次数】   ← 变成性能问题,不再是对错问题
```

### 3.2 证据 > 记忆 > 推测

空口推理几小时,不如一行日志。四条:

- **能报出处的查询命令,优于肉眼扫文件。** 我有过一套理论解释某件事,一条能报出处的命令直接指出真正的原因在哪个文件第几行——和我的理论毫无关系。理论当场破产,省下继续在错误方向上推演的半天。
- **矛盾的观察最值钱。** 日志说写进去的是对的,直接读磁盘也是对的,**但屏幕上显示的是错的**。矛盾一出现,嫌疑立刻从"存错了"移到"存对了显示错了"——两个方向的工作量差几个小时。
- **沉默也是证据。** 日志里孤零零只剩一行"退出",前面**一个关闭事件都没有**。这个缺失说明它根本没走正常关闭流程——不是它想退,是被外面杀掉的。
- **验证工具自己也有前提。** 工具说"我没看见"不等于"它不存在"——可能只是它没权限看那一类东西。让工具去验之前,先确认它的眼睛对得上被验的对象。

### 3.3 动手之前,先把预言写下来

要做实验验证一个猜想时,**先写死每一条预期**:"如果我对,T1 应该正常、T2 应该报错、T3 应该没反应。"

不写下来,你会不自觉地把看到的任何结果都解释成"符合预期"。写下来了,被证伪就是被证伪——我靠这个当场砍掉过一个本来要提交的错误修复。

配套是**受控实验的纪律**:预置互不相同的哨兵内容,让每次写入都能指认是谁写的;每轮开始前四步复位(杀进程、清缓存、复位哨兵、清日志);先读现场,再开测。

---

## 四 · 讲:把负荷搬给别人,是有代价的

> 解释失败不是听的人笨。是讲的人把自己脑子里的东西**原样倒了出来**。

这一章只有一句话:**先讲一层,等对方跟上,再讲下一层。别一口气全倒出来。**

下面六条是它的展开,每条都来自一次真事故。

**1 · 先复述,再开讲。** 对方的问题里如果有假设、有他自造的词、或者和我预想的方向不一样,先说一句"我理解你问的是 X"。复述错了他一句话就能纠正,成本最低;不复述直接讲,讲完才发现答的是另一个问题,整段作废,还赔上信任。

**2 · 讲一个还在变的东西,每一帧都要标时间。**(最高频的一类事故)

> **最危险的图不是画错的图,是结构正确但没标时间的图。**

```
 我画的:  一张【修好之后】的结构图,但没写"这是修好之后的"
 他读成:  本来就这样
 他推出:  "既然本来就是同一份,那不该受那次改动影响啊"
          ↑ 推理没错,前提错了
 后果:    图是对的,所以两个人都很难发现是从哪一步歪的
```

改成之前 / 之中 / 之后三张图,一帧一张,当场就通了。

**3 · 术语第一次出现就给白话。** 缩写和行话头一次登场,给全称加一句人话。特别小心**汇报状态时顺手夹带的新词**——那种语境下对方不好意思打断,这笔债会滚到好几轮之后才炸。宁可重复定义,不要假设他知道。

**4 · 讲完"它是什么",补一句"它不管什么"。** 只讲它多好,对方会自动往外推它的能力范围。边界那一句话,直接省掉下一轮追问。

**5 · 分层,每层都能停。** 结论一句 → 机制 → 边界 → 例外。想挖的人会自己往下问。两个极端都不行:只给结论,和一次性倒出全部细节。

**6 · 画图之前先问:这张图回答哪个问题。**

| 对方在问 | 该画什么 |
|---|---|
| "怎么变成这样的?" | **时间帧序列**——之前 / 之中 / 之后,一帧一张 |
| "现在是什么结构?" | **窄的竖排层次图**,标题里写明这是哪一帧 |
| "这几个东西什么关系?" | 三角图或对照表,别硬画成流程 |

宽图会让标注和它指的东西不在同一行,归属就断了。表格和图一样是一等公民,该用表就别硬画图。

### 验收:让对方用自己的话讲一遍

我自己就是这么学的——听完复述给对方,让对方抓我的错。所以我反过来也这么做:**逐点批改,不重讲全文。** 对的说对,要拧紧的指出拧紧在哪。全文重讲一遍等于告诉他"你刚才说的我没看",而且负荷翻倍。

**做多目标的活,也是同一条规矩:先分诊,后动手。** 接到"把这些问题都处理一下",先出一份分类清单让对方裁决,再开工。对方中途问一句"你在干嘛",说明这条已经犯了。

---

## 五 · 一张清单

```
 学 □ 有没有一个"全都有"的入口?
    □ 三个月后我要换掉它,带得走什么?
    □ 用完了,我真正用到的是哪几样?——这一步最容易被跳过

 建 □ 这东西需要存在吗?手上已经有了吗?标准库/原生能做吗?
    □ 哪些零件跟地基绑死,哪些能单独换?这条线画出来了吗?
    □ 装错了我还能救回来吗?——决定它能待在多里面
    □ 这件事有几个权威?如果有两个,它们之间隔多久?
    □ 这条路走不通的时候,会有人喊一声吗?

 修 □ 我在修故障,还是在调触发条件?
    □ 这一条我是查出来的,还是想出来的?
    □ 动手之前,预期写下来了吗?

 讲 □ 我讲的东西还在变吗?标时间帧了吗?
    □ 这一层讲完能停吗?还是非得听完全部才有用?
    □ 对方复述的时候,偏差在哪一点?
```

---

## 六 · 这份文档不管什么

按自己的规矩,补一句边界:

- 它管**判断**——该怎么切、该不该加、出事先查哪、讲成什么样。
- 它**不管手艺**:某个语言、某个框架、某条命令怎么用,不在这里,也不该在这里。
- 它**不管怎么把经验变成能力**:怎么提问、怎么把一次踩坑沉淀成下次能直接调用的东西,那是另一套方法。
- 关于认知负荷,这里只留了我自己用得上的那一小块。完整论述见 zakirullin 的 [Cognitive Load is what matters](https://github.com/zakirullin/cognitive-load),那是我见过讲得最好的一篇。

> 代码库里没有任何"让它自己变简单"的力量。唯一能让它变简单的,是你有意识地做的选择。
